import React from 'react';

const styles = {
  color: 'white'
}

export const checkedIcon = (
  <span style={styles}>Chord</span>
);

export const uncheckedIcon = (
  <span style={styles}>Text</span>
);
